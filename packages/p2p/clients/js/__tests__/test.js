/* global test expect */

// const p2p1 = require('../index.js')
// const p2p2 = require('../index.js')

test('placeholder', () => {
  // TODO : difficult to test this library in CI because RTCPeerConnections only exist in the browser.
  // There are node ports, but they are ports, not identical: not ideal for testing.
  expect(true).toBe(true)
})
