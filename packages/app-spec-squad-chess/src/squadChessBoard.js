/*
 * SQUAD CHESS BOARD
 * 
 * Set the player perspective: black || white
 * 
 * Render: Given a state (position, turn, legal moves), render the board from the
 * right player perspective.
 * 
 * Move: Given a drag input, run the takeTurn function and return a state. If 
 * takeTurn returns a state, render that state (it will not return a state if
 * the move is illegal).
 * 
 */